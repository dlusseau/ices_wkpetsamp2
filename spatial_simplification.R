# Generate fishing effort
# both the
pbycatch_area <- function(nareas, evenness_alpha){
  
}

# Mean number of events per boat OR total number of boats could be different by area
effort_by_area <- function(nareas, 
                           effort_unit = "mean.fishing.event.boat", 
                           evenness_alpha){
  areas_vec <- 1:nareas
  if(effort_unit == "mean.fishing.event.boat"){
    #proportion_
  }
}